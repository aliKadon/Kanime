<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animeapp2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".AnimeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_start_5"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_start_5"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeApp2">
        <activity
            android:name=".ui.MainActivityDetail"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>